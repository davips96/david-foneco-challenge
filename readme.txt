This app is set up to work with docker-compose up.

To run, cd to the root directory of the application where the docker-compose file is and run docker-compose up.

When docker is ready, access the app by going to localhost:3000 on your web browser.